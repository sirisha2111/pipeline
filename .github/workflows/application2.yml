name: Application2

on:
    push:
        branches:
            - '*'
        paths:
            - 'Application2/**'
    pull_request:
        branches:
            - '*'
        paths:
            - 'Application2/**'

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: shell_script_Application2
              run: |
                echo "Msg comes from Application2 Project"